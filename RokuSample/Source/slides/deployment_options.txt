Depoyment Options

A Channel can be published one of two ways:
- Public publishing through the Roku Channel store search interface.  This makes sense for most "consumer" applications.
- Private publishing, which creates a secret link into the Roku Store which you then use to install the channel into Roku accounts.